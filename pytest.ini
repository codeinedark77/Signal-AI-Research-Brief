[pytest]
markers =
    live: hits a real external API (currently: GitHub Search). Requires network.
        Run everything except these with: pytest -m "not live"
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*:DeprecationWarning
